version: "3.4"

services:
  api:
    image: ccs/raptus-api
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "2123:2123"
    volumes:
      - ./logs/:/app/logs/
    restart: always